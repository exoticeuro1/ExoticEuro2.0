default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS", 5) %>

production:
  primary: &primary
    <<: *default
    url: <%= ENV["DATABASE_URL"] %>

  cable:
    <<: *primary
