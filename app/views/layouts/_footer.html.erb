
<footer>
        <div class = "top-footer-section">
            <h2> <%= link_to 'Exotic Euro', root_path, class: "link" %></h2>
            <div class="social-media">
            
            <% if @user.facebook.present? %>
                <%= link_to @user.facebook, target: "_blank", rel: "noopener", class: "social-media-icon" do %>
                    <%= raw File.read(Rails.root.join("app", "assets", "images", "facebook-icon.svg")) %>
                <% end %>
            <% else %>
                <%= link_to "#", onclick: "alert('Facebook link hasn\\'t been added yet!'); return false;", class: "social-media-icon" do %>
                    <%= raw File.read(Rails.root.join("app", "assets", "images", "facebook-icon.svg")) %>
                <% end %>
            <% end %>

            <% if @user.instagram.present? %>
               <%= link_to @user.instagram, target: "_blank", rel: "noopener", class:  "social-media-icon" do %>
                    <%= raw File.read(Rails.root.join("app", "assets", "images", "instagram-icon.svg")) %>
                <% end %>
            <% else %>
                <%= link_to "#", onclick: "alert('Instagram link hasn\\'t been added yet!'); return false;", class: "social-media-icon" do %>
                    <%= raw File.read(Rails.root.join("app", "assets", "images", "instagram-icon.svg")) %>
                <% end %>
            <% end %>

            <% if @user.youtube.present? %>
                <%= link_to @user.youtube, target: "_blank", rel: "noopener", class: "social-media-icon" do %>
                    <%= raw File.read(Rails.root.join("app", "assets", "images", "youtube-icon.svg")) %>
                <% end %>
             <% else %>
                <%= link_to "#", onclick: "alert('Youtube link hasn\\'t been added yet!'); return false;", class: "social-media-icon" do %>
                    <%= raw File.read(Rails.root.join("app", "assets", "images", "youtube-icon.svg")) %>
                <% end %>
            <% end %>
            </div>

        </div>

        <div class = "middle-footer-section">
        </div>

    <div class="bottom-footer-section">
        <h4>© 2025 Exotic Euro. All rights reserved.</h4>

        <div class="frame">
            <div class="carousel">
                <div class="change_outer">
                    <div class="change_inner">
                        <div class="element">Designed by</div>
                        <div class="element">Developed by</div>
                        <div class="element">Delivered by</div>
                        <!-- if you still need a duplicate at the end for smooth looping, uncomment: -->
                        <!-- <div class="element">Designed</div> -->
                    </div>
                </div>
                <div class="post">
                    <%= link_to 'FrostBeak', 'https://www.frostbeak.com', class: 'dev-credit dev-credit-text', target: '_blank', rel: 'noopener noreferrer' %>
                </div>
            </div>
        </div>
    </div>

</footer>